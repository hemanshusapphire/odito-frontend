// Renamed to AuditIssueCard (it was always category-agnostic — Page 3
// reuses it for Security issues with no changes needed). Kept as a
// re-export so nothing importing the old name breaks.
export { default } from './AuditIssueCard';
