"use client"

import { UserPagesCard } from "./UserPagesCard"

/**
 * See SubscriptionCreditsCard.jsx — same delegation reasoning.
 */
export function SubscriptionPagesCard({ pages }) {
  return <UserPagesCard pages={pages} />
}
